// import { Context } from "../context"

// export const notificationGenerator = async (addressee_id: string, ctx: Context, options: any) => {
//     const message = `${addressee_id} accepted your buddy request`

//     const notification = await ctx.prisma.notification.create({
//         data:
//     })
// }
