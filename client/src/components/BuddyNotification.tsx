import { Notification } from "../generated/graphql";

interface BuddyNotificationProps {
  data: Notification;
  loading: boolean;
}

const BuddyNotification = (props: BuddyNotificationProps) => {
  return <div></div>;
};

export default BuddyNotification;
