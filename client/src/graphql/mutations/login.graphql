mutation login($input: LoginInput!) {
  login(input: $input) {
    IOutput {
      code
      success
      message
    }
    User {
      id
      email
      username
      profile {
        id
        profile_avatar
      }
    }
    ErrorFieldOutput {
      field
      message
    }
  }
}
