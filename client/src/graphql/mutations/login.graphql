mutation login($input: LoginInput!) {
  login(input: $input) {
    IOutput {
      code
      success
      message
    }
    User {
      id
      email
      username
      profile {
        id
      }
    }
    ErrorFieldOutput {
      field
      message
    }
  }
}
