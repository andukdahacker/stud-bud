mutation changePassword($input: ChangePasswordInput!) {
  changePassword(input: $input) {
    IOutput {
      ...IOutput
    }
    User {
      ...User
    }
    ErrorFieldOutput {
      field
      message
    }
  }
}
