mutation createProfile($input: CreateProfileInput!) {
  createProfile(input: $input) {
    IOutput {
      code
      success
      message
    }
    Profile {
      id
      profile_bio
      profile_avatar
      profile_wallpaper
      profile_interests {
        interest {
          id
          interest_name
        }
      }
    }
  }
}
