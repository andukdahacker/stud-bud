mutation connectBuddy($input: RelationshipInput!) {
  connectBuddy(input: $input) {
    IOutput {
      code
      success
      message
    }
    Relationship {
      requester_id
      addressee_id
      specifier_id
      createdAt
      updatedAt
    }
  }
}
