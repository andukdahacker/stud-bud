mutation respondBuddy($input: RelationshipInput!) {
  respondBuddy(input: $input) {
    IOutput {
      code
      success
      message
    }

    Relationship {
      requester_id
      addressee_id
      specifier_id
      status
    }
  }
}
