mutation respondBuddy($input: RelationshipInput!) {
  respondBuddy(input: $input) {
    IOutput {
      ...IOutput
    }
    relationship {
      ...Relationship
    }
    otherEndRelationship {
      ...Relationship
    }
  }
}
