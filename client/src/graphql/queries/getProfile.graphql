query getProfile($where: ProfileWhereUniqueInput!) {
  getProfile(where: $where) {
    IOutput {
      ...IOutput
    }
    Profile {
      ...Profile
      profile_interests {
        ...ProfileInterest
      }
    }
  }
}
