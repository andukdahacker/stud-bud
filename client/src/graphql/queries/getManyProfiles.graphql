query getManyProfiles($where: GetManyProfilesInput!) {
  getManyProfiles(where: $where) {
    IOutput {
      code
      success
      message
    }
    Profile {
      id
      profile_bio
      profile_avatar
      profile_interests {
        interest {
          interest_name
        }
      }
      user {
        id
        username
        email
      }
    }
  }
}
